import { StyleSheet } from "react-native";

export default StyleSheet.create({
    ratesView: {
        flex: 1
    },
    buttonView: {
        flex: 1,
        alignItems: "center",
        flexDirection: "row",
        justifyContent: "center"
    }
});