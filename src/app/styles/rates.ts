import { StyleSheet } from "react-native";

export default StyleSheet.create({
    ratesView: {
        flex: 1,
        backgroundColor: "#fff"
    },
    buttonView: {
        flex: 1,
        alignItems: "center",
        flexDirection: "row",
        justifyContent: "center"
    }
});