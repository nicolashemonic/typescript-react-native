import { StyleSheet } from "react-native";

export default StyleSheet.create({
	planning: {
		flex: 1,
		backgroundColor: "#fff"
	},
	planningModifyRates: {
		flex: 1,
		alignItems: "center",
		flexDirection: "row",
		justifyContent: "center"
	}
});
